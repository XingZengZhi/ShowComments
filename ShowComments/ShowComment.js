;(function($){
  $.fn.ShowComment = function(options){
    var defaults = {
      //参数和属性
    }

    var options = $.extend(defaults, options);

    this.each(function(){
      //实现各种功能
      var _this = $(this);  //获取调用插件的对象
      
    });

    return this;
  }
})(jquery);
